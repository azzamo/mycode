#!/usr/bin/env python3

hostname = 'MTG'
hostname = input("Please Enter a hostname: ")


if hostname.lower() == 'mtg':
  print("The hostname was found to be", hostname)
  print("The hostname matches expected config", hostname)

print("Existing Script")

